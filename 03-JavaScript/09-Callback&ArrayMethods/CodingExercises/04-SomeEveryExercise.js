const allEvens = (numbers) => numbers.every(num => num % 2 === 0);

// if you want to define this as a traditional Function
// function allEvens(numbers) {
//     return numbers.every(num => num % 2 === 0);
// }git